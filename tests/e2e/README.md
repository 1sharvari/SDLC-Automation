# Playwright automation

```text
fixtures/  # auth and test-data setup
pages/     # page objects
specs/     # user-story traceable test specs
```

Name tests as `<JIRA-KEY>.<capability>.spec.ts` and annotate each acceptance criterion in the test title or metadata.

